import React from 'react';
import { View, Text } from 'react-native';

const CartScreen = () => {
  return (
    <View style={{ width: '100%', height: '100%', backgroundColor: '#ccc'}} >
      <Text>I am Cart Screen</Text>
    </View>
  )
};

export default CartScreen;
